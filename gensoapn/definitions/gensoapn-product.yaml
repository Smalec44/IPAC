product: '1.0.0'

info:
  name: gensoapn
  title: "gensoapn"
  version: "1.0.0"

apis:
  'gensoapn_100':
    $ref: gensoapn.yaml

visibility:
  view:
    type: public
  subscribe:
    type: authenticated

plans:
  default:
    title: Default Plan
    description: Default Plan
    approval: false
    rate-limit:
      value: 100/hour
      hard-limit: false
